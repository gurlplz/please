# Sample IPs for testing - replace with your own list
# One IP per line. Supports CIDR (samples first host).
# Get cloud IPs: python main.py --fetch-cloud-ips --sources ip --discovery-only
#
# Example:
# 192.168.1.1
# 10.0.0.0/24
