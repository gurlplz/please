/**
 * Replicated Pin Module - Entry point
 */

const { ReplicatedPinModule, ReplicatedPin, pinHash } = require('./ReplicatedPinModule');

module.exports = {
  ReplicatedPinModule,
  ReplicatedPin,
  pinHash,
};
